jobs_func () {
	(
	# TODO this still needs to be cut down to a size somehow
	jobs -r | sed -e 's/ *running */:/;/^(pwd/d'
	jobs -s | sed -e 's/ *+ *suspended (.*) */:/'
	) | tr '\n' ' '
}

OUTPUT=jobs_func
# vim: filetype=zsh noexpandtab
